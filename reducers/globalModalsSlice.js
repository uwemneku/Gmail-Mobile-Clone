
import { createSlice } from '@reduxjs/toolkit';

const slice = createSlice({
    name:'globalModals',
    initialState:{
        newMeeting:false,
        loading:false
    },
    reducers: {
        openAccountModal:(state)=>{
            
        }
    }
})