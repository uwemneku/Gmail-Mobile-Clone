
import { createSlice } from '@reduxjs/toolkit';

const slice = createSlice({
    name:globalModals,
    initialState:{
        accounts:false,
        loading:false
    },
    reducers: {
        openAccountModal:(state)=>{
            
        }
    }
})